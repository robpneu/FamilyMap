<?xml version="1.0" encoding="utf-8"?>
<manifest package="edu.byu.robpneu.cs240.familymap"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <!--    &lt;!&ndash; Specify the Fire OS minimum API level. &ndash;&gt;
        <uses-amzn-sdk android:minSdkVersion="3"/>-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_familymap_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.PersonActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ui.MainActivity">
        </activity>

        <activity
            android:name=".ui.MapActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ui.MainActivity">
        </activity>

        <activity
            android:name=".ui.FilterActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ui.MainActivity">
        </activity>

        <activity
            android:name=".ui.SettingActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ui.MainActivity">
        </activity>

        <activity
            android:name=".ui.SearchActivity"
            android:label="@string/app_name"
            android:parentActivityName=".ui.MainActivity">
        </activity>
    </application>

</manifest>
